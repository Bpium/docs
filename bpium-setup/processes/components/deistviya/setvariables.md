---
description: >-
  Сохраняет значения в переменные в нужный момент выполнения сценария.
  Используется, когда нужно запомнить промежуточный результат, задать начальные
  значения или подготовить данные для следующих шагов.
---

# Назначение переменных

## Когда использовать

Используйте **Назначение переменных**, когда нужно сохранить данные под понятным именем для использования в других частях сценария. Типичные примеры:

* Сохранить результат веб-запроса перед его обработкой
* Установить значения по умолчанию перед созданием записи
* Подготовить параметры для следующего компонента
* Записать промежуточные вычисления в переменные, чтобы не повторять формулу




## Настройка компонента

### Название

По умолчанию «Назначение переменных». Можно изменить на своё — например, «Сохранить данные клиента» или «Подготовить параметры запроса».

### Описание

Необязательное поле. Можно добавить комментарий для себя или коллег — например, «Здесь сохраняем рассчитанную итоговую сумму перед отправкой».

### **Переменные**

Выходной параметр. Набор переменных и их значений. Слева указывается название [переменной](https://docs.bpium.ru/processes/scripts/variables), справа — её значение. Значение может быть указано в виде текста (для этого его нужно обернуть в "кавычки") или в формате [выражения](https://docs.bpium.ru/processes/scripts/expression).

{% hint style="info" %}
В качестве переменной  можно указать ключ объекта и данные сохранятся как значения этого ключа.
{% endhint %}

## Пограничные события

![](../../../../.gitbook/assets/boundary_any.png)

Компонент поддерживает 2 типа пограничных событий:

* Ошибка — выход из компонента, если произошла какая-либо ошибка
* Таймаут — выход из компонента, спустя заданное ограничение по времени

Если компонент завершился с ошибкой, но на нем не было пограничного события, то процесс завершается. Сообщение ошибки возвращается в результатах процесса.
